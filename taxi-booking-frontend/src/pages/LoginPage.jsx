const LoginPage = () => {
  return (
    <main className="page-shell">
      <section className="page-card">
        <h1>Login</h1>
        <p>Frontend routing and Redux setup are ready. Login form UI comes in the next step.</p>
      </section>
    </main>
  );
};

export default LoginPage;
