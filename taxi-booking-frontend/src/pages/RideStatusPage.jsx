const RideStatusPage = () => {
  return (
    <main className="page-shell">
      <section className="page-card">
        <h1>Ride Status</h1>
        <p>Live ride tracking route is wired and ready for the booking status polling UI.</p>
      </section>
    </main>
  );
};

export default RideStatusPage;
