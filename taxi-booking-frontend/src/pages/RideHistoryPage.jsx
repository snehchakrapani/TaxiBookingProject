const RideHistoryPage = () => {
  return (
    <main className="page-shell">
      <section className="page-card">
        <h1>Ride History</h1>
        <p>Ride history route is connected and ready for the authenticated history list UI.</p>
      </section>
    </main>
  );
};

export default RideHistoryPage;
